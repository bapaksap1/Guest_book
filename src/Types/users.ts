export type TForm = {
  name: string;
  phoneNumber: string;
  address : string;
  description: string;
}

export type TUser = {
  id: string;
  name: string;
  phoneNumber : string;
  address: string;
  description: string;
  createdAt: string;

}